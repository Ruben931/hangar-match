{
  "name": "sc-ship-finder",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "rebuild-ships": "node scripts/rebuild-ships.mjs",
    "gen-best-pages": "node scripts/gen-best-pages.mjs",
    "fetch-uex": "node scripts/fetch-uex.mjs",
    "desktop": "npm --prefix hangar-match-desktop start",
    "desktop:make": "npm --prefix hangar-match-desktop run make"
  },
  "devDependencies": {
    "vite": "^6.3.5"
  }
}
